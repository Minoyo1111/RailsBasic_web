<%= form_for @moviegroup do |f| %>
  <% if @moviegroup.errors.any? %>
  <ul>
    <% @moviegroup.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  </ul>
<% end %>
  標題
  <%= f.text_field :title %>
  <br>
  描述
  <br>
  <%= f.text_area :description %>
  <br>

  <%= f.submit "Submit", :disable_with => 'Submitting...' %>
  <% end %>

</div>

